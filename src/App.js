import React from "react";
import Navigation from "./components/Navigation";

const App = () => {
  return (
    <React.StrictMode>
      <Navigation />
    </React.StrictMode>
  );
};

export default App;
